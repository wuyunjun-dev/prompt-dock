你是资深 macOS 原生应用工程师。请从当前仓库开始开发一个小型本地 macOS 工具，产品名暂定为 PromptDock。目标是：常驻后台/菜单栏，用于帮助用户撰写、改写、优化给 Codex、Claude Code、Cursor、ChatGPT 等 AI 编程助手使用的提示词。请优先实现 macOS 端 MVP，代码要可编译、可运行、结构清晰、便于后续扩展。

一、总体目标

开发一个 macOS 菜单栏应用。它常驻后台，用户可以通过菜单栏图标或全局快捷键打开一个小型浮窗。用户输入原始想法、需求草稿、报错信息或当前剪贴板内容后，应用调用用户自己配置的模型 API，把内容改写成更清晰、更适合 AI 编程代理执行的提示词。

这个工具不是聊天机器人，而是“提示词撰写与优化器”。核心输出应该是可直接复制给 Codex 的高质量任务提示词。

二、技术栈要求

1. 使用 Swift + SwiftUI + AppKit 开发 macOS 原生应用。
2. 最低支持 macOS 14。
3. 优先不引入第三方依赖。如果确实需要依赖，必须说明原因。
4. 应用形态为菜单栏应用，默认不在 Dock 显示。
5. 使用 async/await 处理网络请求。
6. API Key 必须存入 macOS Keychain，不允许明文存储在 UserDefaults 或本地 JSON 文件。
7. 普通配置可以使用 UserDefaults 或本地 Codable JSON 文件。
8. 历史记录可以先用本地 JSON 文件实现，后续可扩展 SQLite/CoreData。
9. 网络层必须抽象成协议，便于之后接入 OpenAI、Anthropic、Gemini、本地 Ollama 或任意 OpenAI-compatible API。

三、MVP 功能范围

请先实现以下功能，不要做过度复杂的功能。

核心功能：

1. 菜单栏常驻
   - 应用启动后显示菜单栏图标。
   - 点击图标打开主浮窗。
   - 菜单中提供：
     - Open PromptDock
     - Optimize Clipboard
     - Settings
     - Quit

2. 全局快捷键
   - 默认快捷键：Command + Option + P。
   - 按下快捷键后打开主浮窗。
   - 先实现固定快捷键即可；如果时间允许，再做可配置快捷键。
   - 如果全局快捷键实现较复杂，可以用 Carbon RegisterEventHotKey 或 AppKit 方案，但代码要封装清楚。

3. 主浮窗
   主浮窗包含：
   - 输入框：用户输入原始想法或粘贴内容。
   - 模式选择：
     - Codex 开发任务
     - Bug 修复提示词
     - 重构提示词
     - 代码审查提示词
     - 需求拆解提示词
     - 通用提示词优化
   - 目标工具选择：
     - Codex
     - Claude Code
     - Cursor
     - ChatGPT
     - Generic Agent
   - 输出框：显示优化后的提示词。
   - 按钮：
     - Optimize
     - Copy Output
     - Clear
     - Save to History
   - 加载状态、错误提示、取消请求按钮。

4. 剪贴板优化
   - 点击菜单项 Optimize Clipboard 时，读取当前剪贴板文本。
   - 自动填入输入框并执行优化。
   - 优化完成后允许用户一键复制结果。
   - 不要默认覆盖剪贴板，除非用户点击 Copy Output。

5. 设置页
   设置页包含：
   - API Provider 名称
   - Base URL
   - API Key
   - Model Name
   - Temperature
   - Max Tokens
   - 自定义额外 Header，MVP 中可以先不做复杂 UI，预留数据结构即可
   - Test Connection 按钮
   - Save 按钮

6. API 兼容性
   MVP 先实现 OpenAI-compatible Chat Completions API。
   请求格式参考：
   POST {baseURL}/chat/completions

   body:
   {
     "model": "...",
     "messages": [
       {"role": "system", "content": "..."},
       {"role": "user", "content": "..."}
     ],
     "temperature": 0.3,
     "max_tokens": 2000
   }

   需要支持：
   - 自定义 baseURL
   - 自定义 model
   - Bearer token API key
   - 解析 choices[0].message.content
   - 网络错误
   - API 错误
   - JSON 解析错误
   - 空响应错误

7. 提示词优化核心逻辑
   请实现一个 PromptOptimizerService。
   它接收：
   - rawInput
   - mode
   - targetTool
   - userPreferences，可选
   - optionalContext，可选

   它输出：
   - optimizedPrompt
   - shortRationale，可选
   - suggestedNextSteps，可选

   MVP UI 可以主要展示 optimizedPrompt，但数据结构要保留 rationale 和 nextSteps。

四、应用内用于调用模型的系统提示词

请在代码中建立 PromptTemplateManager，内置以下系统提示词。注意：这是 PromptDock 调用模型时使用的提示词，不是用户最终看到的界面文案。

系统提示词：

You are an expert prompt architect for AI coding agents. Your job is to rewrite rough user intentions into precise, executable prompts for coding agents such as Codex, Claude Code, Cursor, ChatGPT, and other software engineering assistants.

Your output must be practical, specific, and directly usable.

When rewriting a prompt:
1. Clarify the objective.
2. Add relevant technical constraints.
3. Ask the agent to inspect the existing repository before changing code.
4. Require minimal, high-confidence changes unless the user asks for a large redesign.
5. Require tests or validation steps.
6. Require the agent to explain important assumptions.
7. Avoid vague language.
8. Avoid overengineering.
9. Preserve the user's original intent.
10. If the input is underspecified, make reasonable assumptions and state them inside the generated prompt.

Default output format:
- Task
- Context
- Requirements
- Constraints
- Implementation Plan
- Validation
- Deliverables

For Codex-specific prompts, emphasize:
- inspect the repository first
- identify existing architecture
- make a plan before editing
- keep changes scoped
- run available tests/build commands
- summarize changed files
- mention any commands that could not be run

Do not include unnecessary commentary before or after the optimized prompt unless explicitly requested.

五、针对不同模式的输出规则

1. Codex 开发任务
   输出应该适合直接粘贴给 Codex。
   格式：
   Task
   Context
   Requirements
   Constraints
   Implementation Plan
   Validation
   Deliverables

2. Bug 修复提示词
   必须包含：
   - reproduce the issue
   - locate root cause
   - propose minimal fix
   - add or update tests if possible
   - avoid unrelated refactors

3. 重构提示词
   必须包含：
   - preserve existing behavior
   - identify current architecture
   - refactor in small steps
   - keep public APIs stable unless necessary
   - run regression tests

4. 代码审查提示词
   必须包含：
   - review for correctness
   - review for edge cases
   - review for security/privacy risks
   - review for maintainability
   - provide prioritized findings

5. 需求拆解提示词
   必须包含：
   - convert idea into milestones
   - separate MVP from later enhancements
   - list assumptions
   - define acceptance criteria

6. 通用提示词优化
   输出清晰、结构化、可执行即可。

六、推荐项目结构

请创建类似下面的结构，具体可根据 Xcode 项目实际情况调整：

PromptDock/
  PromptDockApp.swift
  AppDelegate.swift
  MenuBar/
    MenuBarController.swift
    StatusItemManager.swift
  HotKey/
    HotKeyManager.swift
  Views/
    MainPopoverView.swift
    PromptEditorView.swift
    SettingsView.swift
    HistoryView.swift
  Models/
    APIConfiguration.swift
    PromptMode.swift
    TargetTool.swift
    PromptOptimizationRequest.swift
    PromptOptimizationResult.swift
    HistoryItem.swift
  Services/
    PromptOptimizerService.swift
    ModelClient.swift
    OpenAICompatibleClient.swift
    KeychainService.swift
    ClipboardService.swift
    HistoryStore.swift
    PromptTemplateManager.swift
  Utilities/
    AppError.swift
    Logger.swift

七、数据模型要求

请实现这些核心模型：

APIConfiguration:
- providerName: String
- baseURL: String
- modelName: String
- temperature: Double
- maxTokens: Int
- extraHeaders: [String: String]

API Key 不放在 APIConfiguration 明文结构中，必须通过 KeychainService 存取。

PromptMode:
- codexDevelopment
- bugFix
- refactor
- codeReview
- requirementBreakdown
- generalOptimization

TargetTool:
- codex
- claudeCode
- cursor
- chatGPT
- genericAgent

PromptOptimizationRequest:
- rawInput: String
- mode: PromptMode
- targetTool: TargetTool
- optionalContext: String?
- createdAt: Date

PromptOptimizationResult:
- optimizedPrompt: String
- rationale: String?
- suggestedNextSteps: [String]?

HistoryItem:
- id: UUID
- request: PromptOptimizationRequest
- result: PromptOptimizationResult
- createdAt: Date

八、隐私和安全要求

1. API Key 必须存入 Keychain。
2. 不要把 API Key 写入日志。
3. 不要把用户输入自动上传到任何固定服务，只能发送到用户配置的 API。
4. 历史记录默认本地保存。
5. 设置页提供 Clear History。
6. 设置页提供 Clear API Key。
7. 网络请求错误信息不能暴露完整 Authorization header。
8. 代码中不要硬编码任何真实 API Key。

九、UI 要求

界面简洁，不要做复杂设计。重点是可用性。

主界面布局建议：
- 顶部：模式选择、目标工具选择
- 中部：左右两栏
  - 左侧：原始输入
  - 右侧：优化输出
- 底部：Optimize、Copy Output、Save、Clear
- 设置入口放在菜单栏菜单或窗口右上角

请支持浅色/深色模式。
文本输入框要适合长文本。
输出框支持选择和复制。

十、错误处理

请定义 AppError，并对以下情况显示用户可理解的错误：

- API Key 未配置
- Base URL 无效
- Model Name 未配置
- 输入为空
- 网络连接失败
- API 返回非 2xx
- 响应 JSON 解析失败
- 模型返回空内容
- Keychain 读写失败

十一、测试与验证

请至少实现以下测试或可验证逻辑：

1. PromptTemplateManager 单元测试
   - 不同 mode 和 targetTool 能生成不同的 user prompt。
   - Codex 模式包含 inspect repository、validation、deliverables 等关键要求。

2. APIConfiguration 测试
   - 默认配置合理。
   - 无效 baseURL 能被识别。

3. OpenAICompatibleClient 测试
   - 可以用 mock URLProtocol 或 mock client 测试请求体格式。
   - 确认 Authorization header 不会被日志泄露。

4. HistoryStore 测试
   - 能保存、读取、清空历史记录。

如果当前环境无法运行 Xcode 测试，请说明原因，并仍然保证代码结构可测试。

十二、实现顺序

请按这个顺序开发：

第一阶段：项目骨架
- 创建 macOS SwiftUI app
- 实现菜单栏常驻
- 实现主浮窗
- 实现基本输入输出 UI

第二阶段：配置与安全
- 实现 SettingsView
- 实现 APIConfiguration
- 实现 KeychainService
- 保存/读取配置

第三阶段：模型调用
- 实现 ModelClient 协议
- 实现 OpenAICompatibleClient
- 实现 PromptTemplateManager
- 实现 PromptOptimizerService

第四阶段：工作流
- 实现剪贴板读取
- 实现 Optimize Clipboard
- 实现 Copy Output
- 实现 HistoryStore

第五阶段：打磨与测试
- 错误处理
- 加载状态
- 取消请求
- 单元测试
- README

十三、交付物

完成后请提供：

1. 可编译的 macOS app 源码。
2. README.md，包含：
   - 功能说明
   - 本地运行方式
   - API 配置方式
   - 隐私说明
   - 已知限制
3. 简短说明：
   - 实现了哪些功能
   - 修改了哪些主要文件
   - 如何测试
   - 哪些功能留作后续迭代

十四、重要限制

1. 不要做登录系统。
2. 不要做云同步。
3. 不要做账号管理。
4. 不要接入固定后端。
5. 不要把 API Key 写死。
6. 不要默认上传剪贴板内容；只有用户点击 Optimize 或 Optimize Clipboard 时才发送。
7. 不要过度设计插件系统。
8. 优先保证 MVP 可运行。

十五、验收标准

当我运行应用时，应满足：

1. 应用启动后出现在 macOS 菜单栏。
2. 点击菜单栏图标可以打开 PromptDock 浮窗。
3. 可以输入一段粗糙需求，例如：
   “帮我做一个todo app，要有列表、添加、删除”
4. 选择 Codex 开发任务，然后点击 Optimize。
5. 应用会调用我配置的模型 API。
6. 输出一段结构化、清晰、可直接粘贴给 Codex 的开发提示词。
7. 可以一键复制输出。
8. API Key 存储在 Keychain 中。
9. 历史记录保存在本地。
10. 没有配置 API Key 时，应用给出清晰错误提示，而不是崩溃。

请现在开始实现。先检查当前仓库结构。如果是空仓库，请创建完整项目。如果已有项目，请在现有结构上最小侵入式实现。实现过程中保持代码清晰、分层明确、可测试。